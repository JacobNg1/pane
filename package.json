{
  "name": "weather-workspace",
  "private": true,
  "workspaces": [
    "app"
  ],
  "scripts": {
    "dev": "pnpm --filter weather-app dev",
    "build": "pnpm --filter weather-app build",
    "preview": "pnpm --filter weather-app preview"
  }
}